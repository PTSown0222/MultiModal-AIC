[project]
name = "agentic-ai"
version = "0.1.0"
description = "Temporun2026"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24",
    "open-clip-torch>=2.24",
    "opencv-python-headless>=4.8",
    "pillow>=10.0",
    "torch>=2.1",
    "tqdm>=4.66",
]
